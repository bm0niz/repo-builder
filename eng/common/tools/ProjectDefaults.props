<?xml version="1.0" encoding="utf-8"?>
<Project>
  <Import Project="$(ProjectDefaultsPath)" Condition="Exists('$(ProjectDefaultsPath)')"/>

  <PropertyGroup>
    <IsPackable Condition="'$(MSBuildProjectExtension)' != '.pkgproj'">false</IsPackable>
  </PropertyGroup>
</Project>